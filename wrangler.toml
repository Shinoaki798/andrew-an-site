# Cloudflare Pages — used by `pnpm deploy` (direct upload). The Git-connected
# project in the dashboard ignores this file and uses its own build settings.
name = "andrew-an-site"
pages_build_output_dir = "dist"
compatibility_date = "2026-09-01"
